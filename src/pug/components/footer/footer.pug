-
  var footers = [
    {
      title: "Company",
      description: ["Team", "History", "Contact us", "Locations"],
      link: ["#team", "#history", "#contact-us", "#locations"]
    },
    {
      title: "Resources",
      description: [
        "Resource",
        "Resource name",
        "Another resource",
        "Final resource"
      ],
      link: [
        "#resource",
        "#resource-name",
        "#another-resource",
        "#final-resource"
      ]
    },
    {
      title: "Legal",
      description: ["Privacy policy", "Terms of use"],
      link: ["#privacy-policy", "#terms-of-use"]
    }
  ]

mixin footer(invert)
  footer.footer(class=invert && "invert")
    .container
      .row
        .col-md-3.pa-4
          .logo
            |
            |
            img(src=assets + brand.movie.img alt="logo")/
            |
            |
            h6.title #{brand.movie.projectName}
          p.body-2.show-md-up.text-center &copy; #{brand.movie.footerText}
        .col-md-6.py-0.ps-6.pe-0
          ul.show-sm-down.collapsible
            each footer in footers
              li.accordion-content
                .collapsible-header
                  h6.title.mb-4 #{ footer.title }
                  |
                  |
                  i.material-icons.right.arrow expand_more
                  |
                  |
                .collapsible-body
                  ul
                    each item, index in footer.description
                      li
                        a(href=footer.link[index]) #{item}
          .row.show-md-up.justify-content-evenly
            each footer in footers
              .col-sm-3.pa-4.px-md-0.site-map-item
                h6.title-nav.mb-4 #{footer.title}
                ul
                  each item, index in footer.description
                    li
                      a(href=footer.link[index]) #{item}
        .col-md-3.pe-4.ps-0
          .socmed
            |
            |
            a.btn-icon.waves-effect
              span.ion-social-facebook.icon
            |
            |
            a.btn-icon.waves-effect
              span.ion-social-twitter.icon
            |
            |
            a.btn-icon.waves-effect
              span.ion-social-instagram.icon
            |
            |
            a.btn-icon.waves-effect
              span.ion-social-linkedin.icon
            |
            |
          - var locales = ['ar','de','en','id','pt','zh']
          .select-lang.select-outlined
            |
            |
            select.select#lang_select
              each locale in locales
                option(value=locale selected=locale === doc_lang) #{$t.common[locale]}
            |
            |
      p.body-2.text-center.show-sm-down &copy; #{brand.movie.footerText}